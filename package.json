{
  "name": "hearthos",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "db:generate": "pnpm --filter @hearthos/api db:generate",
    "db:push": "pnpm --filter @hearthos/api db:push",
    "db:seed": "pnpm --filter @hearthos/api db:seed"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.15.0"
}
